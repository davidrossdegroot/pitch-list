export default function HomePage() {
  return (
    <main className="mx-auto max-w-3xl p-6">
      <h1 className="text-3xl font-semibold">DC Problems & Pitches</h1>
      <p className="mt-3 text-gray-600">
        App is running. Next step: wire markdown from /problems and /pitches.
      </p>
    </main>
  );
}
